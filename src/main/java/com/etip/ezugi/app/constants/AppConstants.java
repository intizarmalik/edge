package com.etip.ezugi.app.constants;

public class AppConstants {
    public final static String INVALID_JWT_TOKEN = "Invalid JWT token";
    public final static String JWT_TOKEN_EXPIRED = "JWT token is expired";
    public final static String JWT_TOKEN_UNSUPPORTED = "JWT token is unsupported";
    public final static String JWT_TOKEN_CLAIMS_EMPTY = "JWT claims string is empty";
    public final static String INVALID_JWT_SIGNATURE = "Invalid JWT signature";
}
